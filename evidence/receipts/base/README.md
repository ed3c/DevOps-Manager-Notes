# Base receipts

Runtime receipts are generated for exact subjects; templates or directories are not PASS evidence.

Issue #2 expects separate outcomes for:

- Python deterministic tests;
- PostgreSQL migration/integration;
- container build/run;
- Kubernetes render/admission;
- kind/Kubernetes deployment and business verification.

The first four can be exercised in GitHub Actions at bounded evidence ceilings. The kind/Kubernetes live lane remains `NOT_EXERCISED` until a Local Handoff receipt exists.

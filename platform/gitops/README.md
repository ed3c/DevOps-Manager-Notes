# GitOps contract

Argo CD is the desired/observed reconciliation adapter. This template is design/implementation material only until a real Argo CD instance syncs an exact revision and emits a receipt.

`targetRevision` must be replaced by an exact commit SHA for evidence runs. Promotion/rollback through Argo Rollouts is owned by issue #7.
